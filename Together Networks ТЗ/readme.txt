Верстка должна быть на flexbox(никаких флоатов) должна корректно отображаться на всех топовых разрешениях экранов по клику на кнопку Log in открывается логин форма (в скрытых слоях psd) по клику на Next меняется шаг (jquery допустимо) (в скрытых слоях psd) в папке картинок должны лежать только основной фон и лого, остальное на css плюсом будет реализация на less(с переменными, миксинами...) стек браузеров: Latest Chrome, Firefox, Safari, Opera, IE11+.

будет бонусом:
- реализовать отправку данных с регформы (post запрос). Запрос отправлять на тестовый урл http://www.mocky.io/v2/5dfcef48310000ee0ed2c281
- обработать ответ (вывод ошибок под полем), то есть валидация

